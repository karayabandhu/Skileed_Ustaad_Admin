version: '3.8'

services:
  admin-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: karayabandhu/skilled-ustaad-admin-frontend:latest
    container_name: admin-frontend
    ports:
      - "8080:80"
    restart: unless-stopped
    # Environment variables can be defined here
    # environment:
    #   - VITE_API_URL=http://your-backend-api.com
